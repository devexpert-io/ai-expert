# 1. Investigar - Fundamentos de LLMs

Este módulo contiene ejemplos prácticos en Python para entender los conceptos fundamentales del funcionamiento de los Modelos de Lenguaje de Gran Escala (LLMs).

## 📋 Requisitos Previos

- Python 3.8 o superior
- pip (gestor de paquetes de Python)

## 🚀 Configuración del Entorno

1. **Crear un entorno virtual** (recomendado):
   ```bash
   python -m venv venv
   source venv/bin/activate  # En Windows: venv\Scripts\activate
   ```

2. **Instalar dependencias**:
   ```bash
   pip install -r requirements.txt
   ```